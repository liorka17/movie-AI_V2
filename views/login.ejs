<!DOCTYPE html>
<html lang="he">
<%- include('partials/head') %> <!-- חיבור ה-HEAD -->

<body>
    <%- include('partials/navbar') %> <!-- תפריט ניווט -->

    <main class="login-container">
        <div class="login-box">
            <h2>🔑 התחברות למועדון הקולנוע</h2>
            <p class="login-subtitle">🎥 התחבר כדי ליהנות מהמלצות אישיות על הסרטים הטובים ביותר!</p>

            <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
                <p class="error-message"><%= errorMessage %></p>
            <% } %>

            <form action="/user/login" method="POST">
                <div class="input-group">
                    <label for="email">📧 כתובת האימייל</label>
                    <input type="email" id="email" name="email" required>
                </div>

                <div class="input-group">
                    <label for="password">🔒 סיסמה</label>
                    <input type="password" id="password" name="password" required>
                </div>

                <button type="submit" class="btn-login">🎬 התחבר עכשיו</button>
            </form>

            <p class="register-link">אין לך חשבון? <a href="/register">✨ הרשם כאן</a></p>
        </div>
    </main>

    <%- include('partials/footer') %> <!-- פוטר -->
</body>
</html>
